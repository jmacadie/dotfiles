[hostname]
disabled = true

[git_status]
format = '([$all_status$ahead_behind]($style) )'
up_to_date = '[✓](green)'
untracked = '🤷'
stashed = '📦'
modified = '📝'
staged = '[++\($count\)](green)'
ahead = '⇡${count}'
diverged = '⇕⇡${ahead_count}⇣${behind_count}'
behind = '⇣${count}'
